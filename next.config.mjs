/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["pictures.nubecosmica.com.ar"], // Agrega aquí tu dominio
  },
};

export default nextConfig;
